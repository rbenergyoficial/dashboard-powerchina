node_modules/
data/irr_seed.json
data/irr_hora_seed.json

# documentacao de trabalho do Claude — fica local, o repo e PUBLICO
CLAUDE.md

# resto de npm i local — nao entra no repo: um lockfile aqui muda o que TODOS os workflows instalam
package.json
package-lock.json
node_modules/

# ⚠️ A regra acima vale para a RAIZ, e casa em qualquer nivel. O relogio e um app proprio: o
# package.json dele e o manifesto do Function App e TEM de subir, senao o deploy instala nada e
# falha com ENOENT. O lockfile fica junto para o que sobe ser o que foi testado. Nada disto
# alcanca os workflows do repo, que continuam instalando por nome e versao.
!relogio/package.json
!relogio/package-lock.json
